#include <stdio.h>
int main()
{
    int a;
    printf("Enter a number: ");
    scanf("%d",&a);
    printf("The factorial of %d is %d",n,fac(a));
}
int fac(int a)
{
    if(a<=2)
    {
        return a;
    }
    else
    {
    return (a*fac(a-1));
}

}